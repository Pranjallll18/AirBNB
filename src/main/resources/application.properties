spring.application.name=AirBnb

# DB Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://root:lScbGoxjmhHrHMxRNcLFFCuJJdrHzdyr@mysql.railway.internal:3306/railway}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:lScbGoxjmhHrHMxRNcLFFCuJJdrHzdyr}
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}

# Server Configuration
server.servlet.context-path=/api/v1
server.port=${PORT:8081}

# JWT Secret
jwt.secretKey=${JWT_SECRET_KEY:very_long_random_secret_key_here_123456789}

# Frontend URL
frontend.url=${FRONTEND_URL:http://127.0.0.1:5500}

# Stripe Configuration
stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_placeholder}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_test_secret}

# Swagger Configuration
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
